    <header>
      <div class="container-fluid">
        <div class="row">
          <div class="col-12">
            <img src="img/logo_square_white.png" />
            <h1>{{page.title}}</h1>
                {{#if page.subtitle }}
                <h2>{{page.subtitle}}</h2>
                {{/if}}
          </div>
        </div>
      </div>
    </header>